export interface ILogo {
  id: number;
  src: string;
  title: string;
  created?: string;
  updated?: string;
}

export interface ICustomHead {
  title?: string;
  description?: string;
}
